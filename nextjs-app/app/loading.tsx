export default function Loading() {
  console.log('loading...');

  //   const result = await UserService.getLoginUserInfo();
  //   const parsedData = LoginUserInfoDtoSchema.parse(result.data);
  //   console.log(parsedData);

  return <div>페이지 로딩 중...</div>;
}
